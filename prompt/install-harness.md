# install-harness

This file is kept as a transitional location.

Use the canonical installer document instead:

```text
installer/install-harness.md
```

The installer now installs `core/` and generates runtime bridges. It should not scaffold a new harness implementation inside each target repository.
