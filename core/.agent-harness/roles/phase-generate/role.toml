name = "phase-generate"
description = "Implements exactly one planned phase from existing artifacts, validates it when possible, and records the result without replanning."
tools_policy = "read-write"
sandbox_mode = "workspace-write"
