name = "phase-clarify"
description = "Turns an ambiguous work request into scope, decisions, assumptions, non-goals, and done conditions before planning or coding."
tools_policy = "read-only"
sandbox_mode = "read-only"
